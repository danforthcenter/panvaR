% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_scores.R
\name{make_scores}
\alias{make_scores}
\title{Make scores}
\usage{
make_scores(input.df, cols, directions, weights = NULL)
}
\arguments{
\item{input.df}{data.frame, table with only snps that are to be included.
Must have "marker.ID" column}

\item{cols}{character, vector of column names indicating which variables to included in the score}

\item{directions}{numeric, a vector indicating which direction is to be considered more indicative
of an association. 1 indicates higher is better, -1 indicates lower is better. The order should correspond
with the order in cols.}

\item{weights}{numeric, a vector indicating weights for the variables. These must add up to 1.}
}
\value{
A table with a score for each marker based on aggregating the variables indicated.
\enumerate{
\item Variables are first made negative if indicated by \code{directions} vector.
\item Min/max normalization is applied to put all variables on same scale.
\item A weighted mean based on the \code{weights} vector is taken of the normalized variables and output as the final score.
}
}
\description{
Make scores
}
\examples{
# work in progress
}
