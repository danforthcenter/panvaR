% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pc_scree.R
\name{plot_pc_scree}
\alias{plot_pc_scree}
\title{Calculate and plot pcs}
\usage{
plot_pc_scree(bedfile.path, num.pcs = 10, cumulative = F)
}
\arguments{
\item{bedfile.path}{character, path to bedfile (include .bed extension)}

\item{num.pcs}{numeric, total number of pcs to plot}

\item{cumulative}{boolean, if T will plot the cumulative proportion of variance}
}
\value{
GGplot of the given plot.

The output is sensitive to the number of PC's indicated. The program used to calculate
PCs returns singular values only for components indicated. So the proportion can only calculate using
the variance contained in these components. Consequently, shouldn't set the num.pcs too low, this could lead to strange results.
}
\description{
Calculate and plot pcs
}
\examples{
# get input
bedfile.path <- system.file(
  "extdata",
  "Setaria_shattering_example_pruned.bed",
  package = "panvaR")

# plot
plot_pc_scree(
  bedfile.path = bedfile.path)
}
