% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ld_in_window.R
\name{get_ld_in_window}
\alias{get_ld_in_window}
\title{Get linkage disequilibrium}
\usage{
get_ld_in_window(
  qtl.df = NULL,
  tag.snp = NULL,
  window,
  plink.path = NULL,
  geno.bed,
  in.dir,
  out.dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{qtl.df}{data.frame, table that includes list of snps to calculate LD to with columns (CHR, POS, LOGPVAL), corresponding to (chromosome, physical position, and -log10(p-value)).
QTL are typically defined as hits grouped by LD by something like \code{plink --clump}}

\item{tag.snp}{character, marker.ID of snp around which to calculate LD. In the form 'CHR-POS'}

\item{window}{numeric, a physical distance to determine which snps to calculate LD for.
Snps within this distance to the tag.snp or qtl.df snps will be calculated.
The entire size of the region centered on the tag snp within which LD will be calculated is 2*window.}

\item{plink.path}{character, optional, path to plink2 executable. Will overide option set by \link{set_plink_path}.}

\item{geno.bed}{character, prefix of genotype files in plink (bed/bim/fam) format. Do not include ".bed" extension.}

\item{in.dir}{character, directory where genotype files are located}

\item{out.dir}{character, where to output some temporary files.}

\item{verbose}{boolean, if TRUE, output some status reports}
}
\value{
Named list with 2 items
\itemize{
\item table: table with marker.IDs (CHR-POS) and maximum LD in R2 for each snp to the snps in the qtl.df or LD to tag.snp
\item key.snp: marker.ID corresponding to the middle of the window, either the max(LOGPVAL) of qtl.df or tag.snp. useful to retain for downstream functions.
\item qtl.snps: marker.ID corresponding to the markers in the qtl.df. useful to retain for downstream functions.
}
}
\description{
Get LD from a group of snps or a single tag snp to all snps in a window.
}
\details{
When supplying a group a snps, the ld retained for a given snp is the maximum LD of the given snp to the snps in the group.
}
\examples{
# set up inputs for example
plink.path <- bigsnpr::download_plink2()
tag.snp <- "Chr_05-6857045"
geno.bed <- "Setaria_shattering_example_pruned.bed"
out.dir <- file.path(tempdir(), "panvar_ex")
dir.create(out.dir, showWarnings = FALSE)

# run example
out <- get_ld_in_window(
    tag.snp = tag.snp,
    window = 25,
    plink.path = plink.path,
    geno.bed = geno.bed,
    in.dir = system.file("extdata", package="panvaR"),
    out.dir = out.dir)

# list
names(out)
# table
head(out$table)

# clean up
unlink(out.dir, recursive = TRUE)

}
\references{
Chang CC, Chow CC, Tellier LCAM, Vattikuti S, Purcell SM, Lee JJ (2015) Second-generation PLINK: rising to the challenge of larger and richer datasets. GigaScience, 4.
}
